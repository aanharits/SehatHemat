generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model ingridients {
  id       BigInt  @id
  name     String?
  category String?
  price    BigInt?
  protein  Float?
  calories BigInt?
  unit     String?
}
